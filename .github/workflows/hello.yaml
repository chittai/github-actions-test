name: Hello

on: 
  workflow_dispatch:
    inputs:
      greeting:
        type: string
        required: true
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Run
        run: echo "Hello, ${{ inputs.greeting }}!"